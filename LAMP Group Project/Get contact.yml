info:
  name: Get contact
  type: http
  seq: 7

http:
  method: GET
  url: "{{UrlBase}}?id=11"
  headers:
    - name: Authorization
      value: Bearer 9
  params:
    - name: id
      value: "11"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
