info:
  name: List user contacts
  type: http
  seq: 5

http:
  method: GET
  url: "{{UrlBase}}"
  headers:
    - name: Authorization
      value: Bearer 9
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
