info:
  name: Disable user
  type: http
  seq: 14

http:
  method: GET
  url: "{{UrlBase}}?action=disableUser&id=14"
  params:
    - name: action
      value: disableUser
      type: query
    - name: id
      value: "14"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
